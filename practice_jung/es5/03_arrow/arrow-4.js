'use strict';

/*
let sports = () => {
    try {
        let args = arguments;
    } catch (e) {
        console.log('사용 불가');
    }
}
sports(1, 2);*/

/*//ES6 에서 arguments 사용법은 (...Parameter)
let sports = (...args) => {
    try {
        //let args = arguments;
        console.log(args);
    } catch (e) {
        console.log('사용 불가');
    }
}
sports(1, 2);*/